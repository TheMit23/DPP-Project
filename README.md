# Graph k-degree Anonymization (SIGMOD'08)

Implementation of degree anonymization and graph construction algorithms for k-degree anonymity.

## Setup
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt